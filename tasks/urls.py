# ============================================================
# ФАЙЛ: tasks/urls.py
# НАЗНАЧЕНИЕ: URL-маршруты приложения tasks.
#   Подключается из корневого django_project/urls.py.
# ============================================================

from django.urls import path  # Функция создания маршрута URL
from . import views            # Обработчики из tasks/views.py

# ---------------------------------------------------------------
# БЛОК МАРШРУТОВ ПРИЛОЖЕНИЯ
# ---------------------------------------------------------------
urlpatterns = [

    # --- Главная страница ---
    path('', views.index, name='index'),
    # ^ GET / — показывает результаты обработки тестовых файлов
    # ^ POST / — обрабатывает файлы, загруженные пользователем через форму

    # --- Очистка загруженных файлов ---
    path('clean/', views.clean_uploads, name='clean_uploads'),
    # ^ POST /clean/ — удаляет все файлы из uploads/ и сгенерированные графики
]
